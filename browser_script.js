const codeUtils = "/**\n * ============================================================\n * Utils.gs — Utility Functions\n * ============================================================\n * ฟังก์ชันช่วยเหลือต่างๆ\n * - JSON Response\n * - UUID Generator\n * - HTML Escape\n * - Thai Day of Week\n * - Google Drive URL Converter\n * ============================================================\n */\n\n/**\n * jsonResponse — สร้าง JSON response สำหรับ ContentService\n * @param {object} data — ข้อมูลที่ต้องการส่ง\n * @returns {TextOutput}\n */\nfunction jsonResponse(data) {\n  const output = ContentService\n    .createTextOutput(JSON.stringify(data))\n    .setMimeType(ContentService.MimeType.JSON);\n  return output;\n}\n\n/**\n * errorResponse — สร้าง error response\n * @param {string} message — ข้อความ error\n * @param {number} code — HTTP status code (ใช้เป็น reference เท่านั้น)\n * @returns {TextOutput}\n */\nfunction errorResponse(message, code) {\n  return jsonResponse({\n    success: false,\n    error: message,\n    code: code || 500\n  });\n}\n\n/**\n * generateId — สร้าง unique ID\n * @returns {string}\n */\nfunction generateId() {\n  return Utilities.getUuid();\n}\n\n/**\n * escapeHtml — ป้องกัน XSS\n * @param {string} str\n * @returns {string}\n */\nfunction escapeHtml(str) {\n  if (!str) return '';\n  return String(str)\n    .replace(/&/g, '&amp;')\n    .replace(/</g, '&lt;')\n    .replace(/>/g, '&gt;')\n    .replace(/\"/g, '&quot;')\n    .replace(/'/g, '&#039;');\n}\n\nlet _cachedSpreadsheetTimeZone = null;\n\n/**\n * getSpreadsheetTimeZoneCached — ดึง Timezone ของ Spreadsheet พร้อม Cache\n * @returns {string}\n */\nfunction getSpreadsheetTimeZoneCached() {\n  return \"Asia/Bangkok\";\n}\n\n/**\n * getAdjustedDate — แปลง Date object ให้ตรงกับ Timezone ของ Spreadsheet\n * @param {Date} date\n * @returns {Date}\n */\nfunction getAdjustedDate(date) {\n  const d = date || new Date();\n  const tz = getSpreadsheetTimeZoneCached();\n  const formattedStr = Utilities.formatDate(d, tz, \"yyyy/MM/dd HH:mm:ss\");\n  return new Date(formattedStr);\n}\n\n/**\n * parseLocalDateString — แปลงสตริง YYYY-MM-DD ให้เป็น Date object ในเครื่อง (Local Script) โดยไม่ถูก timezone เลื่อน\n * @param {string} dateStr\n * @returns {Date}\n */\nfunction parseLocalDateString(dateStr) {\n  if (!dateStr) return getAdjustedDate();\n  const parts = String(dateStr).split('-');\n  if (parts.length !== 3) return getAdjustedDate();\n  return new Date(Number(parts[0]), Number(parts[1]) - 1, Number(parts[2]));\n}\n\n/**\n * getThaiDayOfWeek — แปลงวันเป็นภาษาไทย\n * @param {Date} date — วันที่ (optional, default = วันนี้)\n * @returns {string} — จันทร์, อังคาร, พุธ, พฤหัสบดี, ศุกร์, เสาร์, อาทิตย์\n */\nfunction getThaiDayOfWeek(date) {\n  const d = date || getAdjustedDate();\n  const days = ['อาทิตย์', 'จันทร์', 'อังคาร', 'พุธ', 'พฤหัสบดี', 'ศุกร์', 'เสาร์'];\n  return days[d.getDay()];\n}\n\n/**\n * getThaiDate — แปลงวันที่เป็นรูปแบบไทย\n * @param {Date} date\n * @returns {string}\n */\nfunction getThaiDate(date) {\n  const d = getAdjustedDate(date);\n  const months = [\n    'มกราคม', 'กุมภาพันธ์', 'มีนาคม', 'เมษายน', 'พฤษภาคม', 'มิถุนายน',\n    'กรกฎาคม', 'สิงหาคม', 'กันยายน', 'ตุลาคม', 'พฤศจิกายน', 'ธันวาคม'\n  ];\n  const day = d.getDate();\n  const month = months[d.getMonth()];\n  const year = d.getFullYear() + 543; // พ.ศ.\n  return day + ' ' + month + ' ' + year;\n}\n\n/**\n * convertDriveUrl — แปลง Google Drive URL เป็น direct link\n * @param {string} url — Google Drive sharing URL\n * @returns {string} — direct image URL\n */\nfunction convertDriveUrl(url) {\n  if (!url) return '';\n\n  // Pattern: https://drive.google.com/file/d/FILE_ID/view\n  let match = url.match(/\\/file\\/d\\/([a-zA-Z0-9_-]+)/);\n  if (match) {\n    return 'https://drive.google.com/uc?export=view&id=' + match[1];\n  }\n\n  // Pattern: https://drive.google.com/open?id=FILE_ID\n  match = url.match(/[?&]id=([a-zA-Z0-9_-]+)/);\n  if (match) {\n    return 'https://drive.google.com/uc?export=view&id=' + match[1];\n  }\n\n  // ถ้าเป็น URL อื่นให้คืนค่าเดิม\n  return url;\n}\n\n/**\n * formatDate — แปลงวันที่เป็น YYYY-MM-DD\n * @param {Date} date\n * @returns {string}\n */\nfunction formatDate(date) {\n  const d = getAdjustedDate(date);\n  const year = d.getFullYear();\n  const month = String(d.getMonth() + 1).padStart(2, '0');\n  const day = String(d.getDate()).padStart(2, '0');\n  return year + '-' + month + '-' + day;\n}\n\n/**\n * getTodayDate — วันที่วันนี้เป็น YYYY-MM-DD\n * @returns {string}\n */\nfunction getTodayDate() {\n  return formatDate(new Date());\n}\n\n/**\n * PERIODS — ข้อมูลคาบเรียนทั้ง 8 คาบ\n */\nconst PERIODS = [\n  { period: 1, start: '09:00', end: '09:50', note: '' },\n  { period: 2, start: '09:50', end: '10:40', note: '' },\n  { period: 3, start: '10:40', end: '11:30', note: '' },\n  { period: 4, start: '11:30', end: '12:30', note: 'รับประทานอาหาร' },\n  { period: 5, start: '12:30', end: '13:20', note: '' },\n  { period: 6, start: '13:20', end: '14:10', note: '' },\n  { period: 7, start: '14:10', end: '15:00', note: '' },\n  { period: 8, start: '15:00', end: '15:50', note: '' }\n];\n\n/**\n * getPeriodInfo — ดึงข้อมูลคาบตามหมายเลข\n * @param {number} periodNum\n * @returns {object|null}\n */\nfunction getPeriodInfo(periodNum) {\n  return PERIODS.find(p => p.period === Number(periodNum)) || null;\n}\n\n/**\n * getCurrentPeriod — หาคาบปัจจุบัน\n * @returns {number|null}\n */\nfunction getCurrentPeriod() {\n  const now = getAdjustedDate();\n  const currentTime = String(now.getHours()).padStart(2, '0') + ':' + String(now.getMinutes()).padStart(2, '0');\n\n  for (const p of PERIODS) {\n    if (currentTime >= p.start && currentTime < p.end) {\n      return p.period;\n    }\n  }\n  return null;\n}\n\n/**\n * validateInput — ตรวจสอบ input ว่าไม่ว่างเปล่า\n * @param {object} fields — { fieldName: value, ... }\n * @returns {string|null} — error message หรือ null ถ้าผ่าน\n */\nfunction validateInput(fields) {\n  for (const [name, value] of Object.entries(fields)) {\n    if (value === undefined || value === null || String(value).trim() === '') {\n      return 'กรุณากรอก ' + name;\n    }\n  }\n  return null;\n}\n\n/**\n * parseSheetDate — แปลงข้อมูลวันที่จาก Sheet เป็น YYYY-MM-DD เสมอ\n * @param {*} val\n * @returns {string}\n */\nfunction parseSheetDate(val) {\n  if (!val) return '';\n  \n  // ถ้าเป็นสตริง ISO หรือมีตัว T (เช่น ดึงมาจาก JSON cache) ให้แปลงเป็น Date แล้ว format ด้วยเขตเวลาไทย\n  if (typeof val === 'string' && val.includes('T')) {\n    const d = new Date(val);\n    if (!isNaN(d.getTime())) {\n      return formatDate(d);\n    }\n  }\n\n  if (val instanceof Date) {\n    return formatDate(val);\n  }\n  \n  // ถ้าเป็นสตริง\n  const str = String(val).trim();\n  \n  // Case 1: YYYY-MM-DD\n  let match = str.match(/^(\\d{4})-(\\d{2})-(\\d{2})/);\n  if (match) {\n    return match[0];\n  }\n\n  // Case 2: DD/MM/YYYY หรือ DD-MM-YYYY\n  match = str.match(/^(\\d{1,2})[\\/\\-](\\d{1,2})[\\/\\-](\\d{4})/);\n  if (match) {\n    let d = parseInt(match[1]);\n    let m = parseInt(match[2]);\n    let y = parseInt(match[3]);\n    if (y > 2400) {\n      y = y - 543; // แปลง พ.ศ. -> ค.ศ.\n    }\n    return y + '-' + String(m).padStart(2, '0') + '-' + String(d).padStart(2, '0');\n  }\n\n  // Case 3: YYYY/MM/DD\n  match = str.match(/^(\\d{4})[\\/\\-](\\d{1,2})[\\/\\-](\\d{1,2})/);\n  if (match) {\n    let y = parseInt(match[1]);\n    let m = parseInt(match[2]);\n    let d = parseInt(match[3]);\n    if (y > 2400) {\n      y = y - 543; // แปลง พ.ศ. -> ค.ศ.\n    }\n    return y + '-' + String(m).padStart(2, '0') + '-' + String(d).padStart(2, '0');\n  }\n\n  return str;\n}\n";
const codeAttendance = "/**\n * ============================================================\n * Attendance.gs — ระบบเช็คชื่อนักเรียน\n * ============================================================\n * - ดึงสถานะการเช็คชื่อแต่ละคาบ (สำหรับแสดง Cards)\n * - ดึงรายชื่อนักเรียน + สถานะเช็คชื่อ\n * - บันทึกเช็คชื่อ (ป้องกัน duplicate)\n * ============================================================\n */\n\n/**\n * getAttendanceCards — ดึงสถานะการเช็คชื่อแต่ละคาบวันนี้\n * @param {string} teacherId\n * @param {string} date — YYYY-MM-DD (optional, default = วันนี้)\n * @returns {TextOutput}\n */\nfunction getAttendanceCards(teacherId, date) {\n  try {\n    const targetDate = date || getTodayDate();\n    const today = getThaiDayOfWeek(parseLocalDateString(targetDate));\n    const schedules = getSheetData('Schedule');\n    const attendance = getSheetData('Attendance');\n    const students = getSheetData('Students');\n    const currentPeriod = getCurrentPeriod();\n\n    // กรองตารางสอนวันนี้ของครู\n    const todaySchedules = schedules\n      .filter(s =>\n        String(s.teacher_id) === String(teacherId) &&\n        String(s.day_of_week).trim() === today\n      )\n      .sort((a, b) => Number(a.period) - Number(b.period));\n\n    // สร้าง cards\n    const cards = todaySchedules.map(schedule => {\n      const periodInfo = getPeriodInfo(schedule.period);\n\n      // นับจำนวนนักเรียนทั้งหมดในห้อง\n      const classStudents = students.filter(st =>\n        String(st.class_room).trim() === String(schedule.class_name).trim() &&\n        st.status === 'active'\n      );\n      const totalStudents = classStudents.length;\n\n      // ดูว่าเช็คชื่อแล้วหรือยัง\n      const attendanceRecords = attendance.filter(a =>\n        String(a.schedule_id) === String(schedule.schedule_id) &&\n        parseSheetDate(a.date) === targetDate\n      );\n\n      const isChecked = attendanceRecords.length > 0;\n      const presentCount = attendanceRecords.filter(a => a.status === 'มา').length;\n\n      return {\n        scheduleId: schedule.schedule_id,\n        className: schedule.class_name,\n        subjectName: schedule.subject_name,\n        period: Number(schedule.period),\n        startTime: periodInfo ? periodInfo.start : schedule.start_time,\n        endTime: periodInfo ? periodInfo.end : schedule.end_time,\n        isCurrent: currentPeriod === Number(schedule.period),\n        isChecked: isChecked,\n        presentCount: presentCount,\n        totalStudents: totalStudents\n      };\n    });\n\n    return jsonResponse({\n      success: true,\n      data: {\n        date: targetDate,\n        dayOfWeek: today,\n        currentPeriod: currentPeriod,\n        cards: cards\n      }\n    });\n  } catch (error) {\n    Logger.log('getAttendanceCards error: ' + error.message);\n    return errorResponse('ไม่สามารถโหลดข้อมูลเช็คชื่อได้');\n  }\n}\n\n/**\n * getStudentsForAttendance — ดึงรายชื่อนักเรียนสำหรับเช็คชื่อ\n * @param {string} scheduleId\n * @param {string} date — YYYY-MM-DD\n * @returns {TextOutput}\n */\nfunction getStudentsForAttendance(scheduleId, date) {\n  try {\n    const targetDate = date || getTodayDate();\n    const schedules = getSheetData('Schedule');\n    const students = getSheetData('Students');\n    const attendance = getSheetData('Attendance');\n\n    // หา schedule\n    const schedule = schedules.find(s => String(s.schedule_id) === String(scheduleId));\n    if (!schedule) {\n      return errorResponse('ไม่พบข้อมูลตารางสอน');\n    }\n\n    // ดึงนักเรียนในห้อง\n    const classStudents = students\n      .filter(st =>\n        String(st.class_room).trim() === String(schedule.class_name).trim() &&\n        st.status === 'active'\n      )\n      .sort((a, b) => {\n        // เรียงตาม prefix + ชื่อ\n        const nameA = (a.first_name || '') + (a.last_name || '');\n        const nameB = (b.first_name || '') + (b.last_name || '');\n        return nameA.localeCompare(nameB, 'th');\n      });\n\n    // ดึง attendance ที่มีอยู่แล้ว\n    const existingAttendance = attendance.filter(a =>\n      String(a.schedule_id) === String(scheduleId) &&\n      parseSheetDate(a.date) === targetDate\n    );\n\n    // Map นักเรียน + สถานะเช็คชื่อ\n    const studentList = classStudents.map((st, index) => {\n      const existing = existingAttendance.find(a =>\n        String(a.student_id) === String(st.student_id)\n      );\n\n      return {\n        studentId: st.student_id,\n        no: index + 1,\n        prefix: st.prefix,\n        firstName: st.first_name,\n        lastName: st.last_name,\n        status: existing ? existing.status : null\n      };\n    });\n\n    const periodInfo = getPeriodInfo(schedule.period);\n\n    return jsonResponse({\n      success: true,\n      data: {\n        schedule: {\n          scheduleId: schedule.schedule_id,\n          className: schedule.class_name,\n          subjectName: schedule.subject_name,\n          period: Number(schedule.period),\n          startTime: periodInfo ? periodInfo.start : schedule.start_time,\n          endTime: periodInfo ? periodInfo.end : schedule.end_time\n        },\n        date: targetDate,\n        students: studentList,\n        isChecked: existingAttendance.length > 0\n      }\n    });\n  } catch (error) {\n    Logger.log('getStudentsForAttendance error: ' + error.message);\n    return errorResponse('ไม่สามารถโหลดรายชื่อนักเรียนได้');\n  }\n}\n\n/**\n * saveAttendance — บันทึกเช็คชื่อนักเรียน\n * @param {string} teacherId\n * @param {string} scheduleId\n * @param {string} date — YYYY-MM-DD\n * @param {object[]} records — [{ studentId, status }, ...]\n * @returns {TextOutput}\n */\nfunction saveAttendance(teacherId, scheduleId, date, records) {\n  try {\n    const err = validateInput({\n      'ตารางสอน': scheduleId,\n      'วันที่': date,\n      'ข้อมูลเช็คชื่อ': records\n    });\n    if (err) return errorResponse(err);\n\n    if (!Array.isArray(records) || records.length === 0) {\n      return errorResponse('ไม่มีข้อมูลเช็คชื่อ');\n    }\n\n    const sheet = getSheet('Attendance');\n    const existingData = sheet.getDataRange().getValues();\n    const headers = existingData[0];\n    const timestamp = new Date().toISOString();\n\n    // หา column indexes\n    const colIdx = {};\n    headers.forEach((h, i) => { colIdx[h] = i; });\n\n    // ลบ records เดิมของ schedule + date เดียวกัน (overwrite)\n    const rowsToDelete = [];\n    for (let i = existingData.length - 1; i >= 1; i--) {\n      if (\n        String(existingData[i][colIdx['schedule_id']]) === String(scheduleId) &&\n        parseSheetDate(existingData[i][colIdx['date']]) === String(date)\n      ) {\n        rowsToDelete.push(i + 1); // 1-indexed for sheet\n      }\n    }\n\n    // ลบจากล่างขึ้นบนเพื่อไม่ให้ index เลื่อน\n    rowsToDelete.forEach(row => {\n      sheet.deleteRow(row);\n    });\n\n    // เพิ่ม records ใหม่\n    const newRows = records.map(r => [\n      generateId(),           // attendance_id\n      date,                   // date\n      teacherId,              // teacher_id\n      scheduleId,             // schedule_id\n      r.studentId,            // student_id\n      escapeHtml(r.status),   // status (มา/ขาด/ลา/สาย)\n      timestamp               // timestamp\n    ]);\n\n    if (newRows.length > 0) {\n      const lastRow = sheet.getLastRow();\n      sheet.getRange(lastRow + 1, 1, newRows.length, newRows[0].length)\n        .setValues(newRows);\n    }\n\n    SpreadsheetApp.flush();\n\n    // ล้างแคชของ Attendance\n    clearSheetCache('Attendance');\n\n    // นับจำนวนมาเรียน\n    const presentCount = records.filter(r => r.status === 'มา').length;\n\n    return jsonResponse({\n      success: true,\n      message: 'บันทึกเช็คชื่อสำเร็จ',\n      data: {\n        totalRecords: records.length,\n        presentCount: presentCount\n      }\n    });\n  } catch (error) {\n    Logger.log('saveAttendance error: ' + error.message);\n    return errorResponse('ไม่สามารถบันทึกเช็คชื่อได้: ' + error.message);\n  }\n}\n";
const codeScore = "/**\n * ============================================================\n * Score.gs — ระบบเก็บคะแนน\n * ============================================================\n */\n\nfunction getClassList(teacherId) {\n  try {\n    const schedules = getSheetData('Schedule');\n    const classes = [...new Set(\n      schedules.filter(s => String(s.teacher_id) === String(teacherId))\n        .map(s => String(s.class_name).trim())\n    )].sort();\n    return jsonResponse({ success: true, data: classes });\n  } catch (error) {\n    return errorResponse('ไม่สามารถโหลดรายชื่อชั้นได้');\n  }\n}\n\nfunction getSubjectList(teacherId, className) {\n  try {\n    const schedules = getSheetData('Schedule');\n    const subjects = [...new Set(\n      schedules.filter(s =>\n        String(s.teacher_id) === String(teacherId) &&\n        String(s.class_name).trim() === String(className).trim()\n      ).map(s => String(s.subject_name).trim())\n    )].sort();\n    return jsonResponse({ success: true, data: subjects });\n  } catch (error) {\n    return errorResponse('ไม่สามารถโหลดรายชื่อวิชาได้');\n  }\n}\n\nfunction getScores(className, subjectName) {\n  try {\n    const students = getSheetData('Students');\n    const scores = getSheetData('Scores');\n    const classStudents = students\n      .filter(st => String(st.class_room).trim() === String(className).trim() && st.status === 'active')\n      .sort((a, b) => ((a.first_name||'')+(a.last_name||'')).localeCompare((b.first_name||'')+(b.last_name||''), 'th'));\n\n    if (classStudents.length === 0) {\n      return jsonResponse({ success: true, data: { students: [], message: 'ไม่พบนักเรียน' } });\n    }\n\n    const studentScores = classStudents.map((st, i) => {\n      const sc = scores.find(s =>\n        String(s.student_id) === String(st.student_id) &&\n        String(s.class_name).trim() === String(className).trim() &&\n        String(s.subject_name).trim() === String(subjectName).trim()\n      );\n      return {\n        no: i + 1, studentId: st.student_id, prefix: st.prefix,\n        firstName: st.first_name, lastName: st.last_name,\n        worksheet1: sc ? sc.worksheet_1 : '', worksheet2: sc ? sc.worksheet_2 : '',\n        worksheet3: sc ? sc.worksheet_3 : '', worksheet4: sc ? sc.worksheet_4 : '',\n        worksheet5: sc ? sc.worksheet_5 : '', worksheetTotal: sc ? sc.worksheet_total : '',\n        midtermScore: sc ? sc.midterm_score : '', midtermExam: sc ? sc.midterm_exam : '',\n        finalExam: sc ? sc.final_exam : '', totalScore: sc ? sc.total_score : '',\n        grade: sc ? sc.grade : ''\n      };\n    });\n    return jsonResponse({ success: true, data: { className, subjectName, students: studentScores } });\n  } catch (error) {\n    return errorResponse('ไม่สามารถโหลดคะแนนได้');\n  }\n}\n\nfunction saveScores(className, subjectName, records) {\n  try {\n    if (!Array.isArray(records) || records.length === 0) return errorResponse('ไม่มีข้อมูลคะแนน');\n    const sheet = getSheet('Scores');\n    const existingData = sheet.getDataRange().getValues();\n    const headers = existingData[0];\n    const colIdx = {};\n    headers.forEach((h, i) => { colIdx[h] = i; });\n\n    records.forEach(record => {\n      const ws = record.worksheets || [];\n      const mx = record.worksheetMaxScores || [];\n      let wsTotal = 0; ws.forEach(w => { wsTotal += Number(w) || 0; });\n      let mxTotal = 0; mx.forEach(m => { mxTotal += Number(m) || 0; });\n      let midScore = (mxTotal > 0 && wsTotal > 0) ? Math.round(((wsTotal * 70) / mxTotal) * 100) / 100 : '';\n      const midExam = Number(record.midtermExam) || 0;\n      const finExam = Number(record.finalExam) || 0;\n      const total = (midScore || 0) + midExam + finExam;\n      const grade = calculateGrade(total);\n      while (ws.length < 5) ws.push('');\n\n      let existingRow = -1;\n      for (let i = 1; i < existingData.length; i++) {\n        if (String(existingData[i][colIdx['student_id']]) === String(record.studentId) &&\n            String(existingData[i][colIdx['class_name']]).trim() === String(className).trim() &&\n            String(existingData[i][colIdx['subject_name']]).trim() === String(subjectName).trim()) {\n          existingRow = i + 1; break;\n        }\n      }\n      const rowData = [\n        existingRow > 0 ? existingData[existingRow-1][colIdx['score_id']] : generateId(),\n        className, subjectName, record.studentId,\n        ws[0]||'', ws[1]||'', ws[2]||'', ws[3]||'', ws[4]||'',\n        wsTotal||'', midScore||'', midExam||'', finExam||'', total||'', grade\n      ];\n      if (existingRow > 0) {\n        sheet.getRange(existingRow, 1, 1, rowData.length).setValues([rowData]);\n      } else {\n        sheet.appendRow(rowData);\n      }\n    });\n    SpreadsheetApp.flush();\n    clearSheetCache('Scores');\n    return jsonResponse({ success: true, message: 'บันทึกคะแนนสำเร็จ (' + records.length + ' คน)' });\n  } catch (error) {\n    return errorResponse('ไม่สามารถบันทึกคะแนนได้: ' + error.message);\n  }\n}\n\nfunction calculateGrade(totalScore) {\n  if (totalScore === '' || totalScore === null || totalScore === undefined) return '';\n  const score = Number(totalScore);\n  if (isNaN(score)) return '';\n  if (score >= 79.5) return 4;\n  if (score >= 74.5) return 3.5;\n  if (score >= 69.5) return 3;\n  if (score >= 64.5) return 2.5;\n  if (score >= 59.5) return 2;\n  if (score >= 54.5) return 1.5;\n  if (score >= 49.5) return 1;\n  return 0;\n}\n";
const codeReport = "/**\n * ============================================================\n * Report.gs — ระบบรายงาน\n * ============================================================\n */\n\nfunction getAttendanceReport(teacherId, className, subjectName, month) {\n  try {\n    const attendance = getSheetData('Attendance');\n    const students = getSheetData('Students');\n    const schedules = getSheetData('Schedule');\n\n    // หา scheduleIds ของครู + ชั้น + วิชา\n    let scheduleIds = schedules\n      .filter(s => String(s.teacher_id) === String(teacherId))\n      .filter(s => !className || String(s.class_name).trim() === String(className).trim())\n      .filter(s => !subjectName || String(s.subject_name).trim() === String(subjectName).trim())\n      .map(s => String(s.schedule_id).trim());\n\n    // กรอง attendance ตาม scheduleIds + month\n    let filtered = attendance.filter(a => scheduleIds.includes(String(a.schedule_id).trim()));\n    if (month) {\n      filtered = filtered.filter(a => {\n        const d = parseSheetDate(a.date);\n        return d.substring(0, 7) === month; // YYYY-MM\n      });\n    }\n\n    // ดึงนักเรียนที่เกี่ยวข้อง\n    const targetClass = className || '';\n    const classStudents = targetClass\n      ? students.filter(st => String(st.class_room).trim() === targetClass && st.status === 'active')\n        .sort((a, b) => ((a.first_name||'')).localeCompare(b.first_name||'', 'th'))\n      : [];\n\n    // สรุปรายนักเรียน\n    const summary = classStudents.map((st, i) => {\n      const stRecords = filtered.filter(a => String(a.student_id) === String(st.student_id));\n      return {\n        no: i + 1,\n        studentId: st.student_id,\n        prefix: st.prefix,\n        firstName: st.first_name,\n        lastName: st.last_name,\n        present: stRecords.filter(r => r.status === 'มา').length,\n        absent: stRecords.filter(r => r.status === 'ขาด').length,\n        leave: stRecords.filter(r => r.status === 'ลา').length,\n        late: stRecords.filter(r => r.status === 'สาย').length,\n        total: stRecords.length\n      };\n    });\n\n    // สรุปรวม\n    const totalPresent = filtered.filter(a => a.status === 'มา').length;\n    const totalAbsent = filtered.filter(a => a.status === 'ขาด').length;\n    const totalLeave = filtered.filter(a => a.status === 'ลา').length;\n    const totalLate = filtered.filter(a => a.status === 'สาย').length;\n\n    return jsonResponse({\n      success: true,\n      data: {\n        className: className || 'ทุกชั้น',\n        subjectName: subjectName || 'ทุกวิชา',\n        month: month || 'ทั้งหมด',\n        students: summary,\n        totals: { present: totalPresent, absent: totalAbsent, leave: totalLeave, late: totalLate, all: filtered.length }\n      }\n    });\n  } catch (error) {\n    Logger.log('getAttendanceReport error: ' + error.message);\n    return errorResponse('ไม่สามารถโหลดรายงานเช็คชื่อได้');\n  }\n}\n\nfunction getScoreReport(className, subjectName) {\n  try {\n    const students = getSheetData('Students');\n    const scores = getSheetData('Scores');\n\n    const classStudents = students\n      .filter(st => String(st.class_room).trim() === String(className).trim() && st.status === 'active')\n      .sort((a, b) => ((a.first_name||'')).localeCompare(b.first_name||'', 'th'));\n\n    const report = classStudents.map((st, i) => {\n      const sc = scores.find(s =>\n        String(s.student_id) === String(st.student_id) &&\n        String(s.class_name).trim() === String(className).trim() &&\n        String(s.subject_name).trim() === String(subjectName).trim()\n      );\n      return {\n        no: i + 1, studentId: st.student_id, prefix: st.prefix,\n        firstName: st.first_name, lastName: st.last_name,\n        worksheetTotal: sc ? sc.worksheet_total : '',\n        midtermScore: sc ? sc.midterm_score : '',\n        midtermExam: sc ? sc.midterm_exam : '',\n        finalExam: sc ? sc.final_exam : '',\n        totalScore: sc ? sc.total_score : '',\n        grade: sc ? sc.grade : ''\n      };\n    });\n\n    // สถิติเกรด\n    const gradeStats = { '4': 0, '3.5': 0, '3': 0, '2.5': 0, '2': 0, '1.5': 0, '1': 0, '0': 0 };\n    report.forEach(r => {\n      if (r.grade !== '') gradeStats[String(r.grade)] = (gradeStats[String(r.grade)] || 0) + 1;\n    });\n\n    return jsonResponse({\n      success: true,\n      data: { className, subjectName, students: report, gradeStats }\n    });\n  } catch (error) {\n    return errorResponse('ไม่สามารถโหลดรายงานคะแนนได้');\n  }\n}\n\nfunction getSettingsData() {\n  try {\n    const settings = getSheetData('Settings');\n    const map = {};\n    settings.forEach(s => { map[s.key] = s.value; });\n    map.logo_url = convertDriveUrl(map.logo_url || '');\n    return jsonResponse({ success: true, data: map });\n  } catch (error) {\n    return errorResponse('ไม่สามารถโหลดการตั้งค่าได้');\n  }\n}\n\nfunction saveSettings(school_name, logo_url, footer_credit) {\n  try {\n    const sheet = getSheet('Settings');\n    const existingData = sheet.getDataRange().getValues();\n    const headers = existingData[0];\n    const colIdx = {};\n    headers.forEach((h, i) => { colIdx[h] = i; });\n\n    const updates = [\n      { key: 'school_name', value: school_name },\n      { key: 'logo_url', value: logo_url },\n      { key: 'footer_credit', value: footer_credit }\n    ];\n\n    updates.forEach(update => {\n      let found = false;\n      for (let i = 1; i < existingData.length; i++) {\n        if (String(existingData[i][colIdx['key']]) === update.key) {\n          sheet.getRange(i + 1, colIdx['value'] + 1).setValue(update.value);\n          found = true;\n          break;\n        }\n      }\n      if (!found) {\n        sheet.appendRow([update.key, update.value]);\n      }\n    });\n\n    clearSheetCache('Settings');\n    return jsonResponse({ success: true, message: 'บันทึกการตั้งค่าสำเร็จ' });\n  } catch (error) {\n    Logger.log('saveSettings error: ' + error.message);\n    return errorResponse('ไม่สามารถบันทึกการตั้งค่าได้: ' + error.message);\n  }\n}\n";

const models = monaco.editor.getModels();
const modelUtils = models.find(m => m.getValue().includes('Utils.gs'));
const modelAttendance = models.find(m => m.getValue().includes('Attendance.gs'));
const modelScore = models.find(m => m.getValue().includes('Score.gs'));
const modelReport = models.find(m => m.getValue().includes('Report.gs'));

if (modelUtils) modelUtils.setValue(codeUtils);
if (modelAttendance) modelAttendance.setValue(codeAttendance);
if (modelScore) modelScore.setValue(codeScore);
if (modelReport) modelReport.setValue(codeReport);
console.log('Completed setting all Monaco models!');